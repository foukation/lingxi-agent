<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/fl_layout"
    android:background="@drawable/voice_bg"
    android:orientation="vertical">
    <ImageView
        android:contentDescription="录音半透明背景"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:scaleType="centerCrop"
        android:layout_height="@dimen/dp_160"/>


    <ImageView
        android:id="@+id/iv_bg"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_gravity="bottom"
        android:contentDescription="录音背景"
        android:layout_marginHorizontal="@dimen/dp_16"
        app:layout_constraintBottom_toBottomOf="parent"
        android:scaleType="fitXY"
        android:src="@drawable/shape_voice_bg" />


    <com.fxzs.lingxiagent.view.common.VoiceButton
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/btn_wx_voice"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_gravity="bottom|center_horizontal" />

    <TextView
        app:layout_constraintBottom_toTopOf="@+id/btn_wx_voice"
        android:id="@+id/rc_audio_state_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dp_16"
        android:gravity="center"
        android:layout_gravity="bottom|center_horizontal"
        android:drawablePadding="10dp"
        android:textColor="@color/color_A0A0A0"
        android:textSize="@dimen/font_12"
        tools:text="松开 发送" />
</androidx.constraintlayout.widget.ConstraintLayout>
